name: Docker Build and Run on Windows

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-run-windows-docker:
    name: Build and Run Docker (Windows)
    runs-on: windows-latest
    strategy:
      matrix:
        config: [Debug, Release]
    env:
      VS_YEAR: 2022
      CMAKE_VERSION: 3.21.3
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Build Base Docker Image
        run: |
          docker build --no-cache -t base -f Dockerfile.windows.base .

      - name: Build Docker Image
        run: |
          docker build --no-cache --build-arg VS_VERSION=${{ env.VS_YEAR }} --build-arg CMAKE_VERSION=${{ env.CMAKE_VERSION }} -t crossplatformapp-windows-vs${{ env.VS_YEAR }} -f Dockerfile.windows.vs .

      - name: Run Build Script Inside Docker
        run: |
          docker run --rm -e CONFIG=${{ matrix.config }} -e VS_YEAR=${{ env.VS_YEAR }} -v "${{ github.workspace }}:C:\app" crossplatformapp-windows-vs${{ env.VS_YEAR }} cmd.exe /c dir C:\scripts\windows && cmd.exe /c C:\scripts\windows\build.vs.cmd

      - name: Run Application Inside Docker
        run: |
          docker run --rm -e CONFIG=${{ matrix.config }} -e VS_YEAR=${{ env.VS_YEAR }} -v "${{ github.workspace }}:C:\app" crossplatformapp-windows-vs${{ env.VS_YEAR }} cmd.exe /c C:\scripts\windows\run.cmd